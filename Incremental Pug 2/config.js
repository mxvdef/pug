const CONFIG = {
    // Click settings
    CLICK_VALUE: 1,
    CLICK_ANIMATION_DURATION: 100,
    
    // Particle settings
    PARTICLE_COUNT: 5,
    PARTICLE_DURATION: 1000,
    PARTICLE_SPAWN_RATE: 40, // particles per second
    MAX_PARTICLES: 100,
    PARTICLE_SIZE_MIN: 50,
    PARTICLE_SIZE_MAX: 90,
    
    // Upgrade settings
    BASE_UPGRADE_COST: 10,
    UPGRADE_COST_MULTIPLIER: 1.15,
    
    // Generator settings
    GENERATOR_TICK_RATE: 1000, // ms
};
